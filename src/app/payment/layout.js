export const metadata = {
  title: "Go Premium",
  description: "Upgrade to PromptVerse Premium for unlimited access to private prompts.",
};

export default function Layout({ children }) {
  return children;
}
