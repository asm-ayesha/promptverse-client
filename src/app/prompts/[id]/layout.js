export const metadata = {
  title: "Prompt Details",
  description: "View the full details, content and reviews for this AI prompt.",
};

export default function Layout({ children }) {
  return children;
}
