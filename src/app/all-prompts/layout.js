export const metadata = {
  title: "Browse Prompts",
  description:
    "Browse and search thousands of high-quality AI prompts across every category and tool.",
};

export default function Layout({ children }) {
  return children;
}
